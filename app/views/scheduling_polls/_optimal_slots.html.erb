<% if poll.response_count > 0 %>
  <div id="optimal_slots" class="mt-8 p-4 bg-cyan-900/20 rounded-lg border border-cyan-700/50">
    <h4 class="text-lg font-semibold text-cyan-400 mb-2">Recommended Time Slots</h4>
    <div class="space-y-2">
      <% poll.optimal_slots(limit: 3).each_with_index do |slot, index| %>
        <div class="flex items-center space-x-3">
          <span class="text-2xl"><%= ["🥇", "🥈", "🥉"][index] %></span>
          <span class="text-white"><%= slot.display_label %></span>
          <span class="text-sm text-gray-400">(<%= slot.available_count %> available, score: <%= slot.availability_score %>%)</span>
        </div>
      <% end %>
    </div>
  </div>
<% end %>