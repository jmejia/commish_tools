<nav class="px-6 py-4 border-b border-gray-800">
  <div class="flex items-center justify-between <%= local_assigns[:nav_container_class] || 'max-w-6xl' %> mx-auto">
    <div class="flex items-center space-x-4">
      <%= link_to root_path, class: "flex items-center hover:opacity-80 transition-opacity" do %>
        <%= image_tag "commish-tools-logo.png", alt: "CommishTools", class: "h-8 w-auto" %>
      <% end %>
      <span class="text-gray-400">|</span>
      <%= link_to leagues_path, class: "text-gray-400 hover:text-white transition-colors" do %>
        My Leagues
      <% end %>
      <% if local_assigns[:league] %>
        <span class="text-gray-400">|</span>
        <%= link_to dashboard_league_path(league), class: "text-gray-400 hover:text-white transition-colors" do %>
          <%= league.name %>
        <% end %>
      <% elsif local_assigns[:league_membership] %>
        <span class="text-gray-400">|</span>
        <%= link_to dashboard_league_path(league_membership.league), class: "text-gray-400 hover:text-white transition-colors" do %>
          <%= league_membership.league.name %>
        <% end %>
      <% end %>
      <% if local_assigns[:page_title] %>
        <span class="text-gray-400">|</span>
        <h1 class="text-xl font-semibold text-white"><%= page_title %></h1>
      <% end %>
    </div>
    
    <div class="flex items-center space-x-4">
      <%= link_to profile_path, class: "text-gray-400 hover:text-white transition-colors" do %>
        👤 My Profile
      <% end %>
      <span class="text-white">Welcome, <%= current_user.first_name %>!</span>
      <%= button_to 'Sign Out', destroy_user_session_path, method: :delete,
          class: 'px-4 py-2 text-white hover:text-red-400 transition-colors bg-transparent border-none cursor-pointer' %>
    </div>
  </div>
</nav>